# Server-only environment variables

# Secret used to authorize indexing endpoints
INDEXER_SECRET=change-me-strong-secret

# Image search controls
# Disable OCR for speed (1=enabled, 0=disabled)
IMAGE_SEARCH_OCR_ENABLED=1
# OCR timeout in milliseconds
IMAGE_SEARCH_OCR_TIMEOUT_MS=5000

# Enable CLIP embeddings + pgvector search (1=enabled, 0=disabled)
IMAGE_SEARCH_EMBEDDINGS_ENABLED=0
IMAGE_SEARCH_URL=http://localhost:9000/search
IMAGE_SEARCH_TEXT_URL=http://localhost:9000/search-text
# Cosine distance threshold (lower = stricter)
IMAGE_SEARCH_EMBEDDING_MAX_DIST=0.35

# Add other existing envs here (database URLs, auth keys, etc.)
# SUPABASE_URL=
# SUPABASE_ANON_KEY=

# NOTE: Do not commit your real .env.local to git. Copy this file to .env.local and fill values.